name: Prettier And Test
run-name: Running Prettier And Tests
on: [push]
jobs:
  PrettierTests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: npm i
      - run: npm run format
      - run: npx playwright install
      - run: npm run test
